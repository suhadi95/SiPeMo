---
deployment:
  tasks:
    - export DEPLOYPATH=/home/suhadipc/public_html/web_sipemo2/
    - /bin/rsync -a --delete --exclude='.git/' --exclude='.env' --exclude='vendor/' --exclude='node_modules/' ./ $DEPLOYPATH
```yaml
---
deployment:
  tasks:
    - export DEPLOYPATH=/home/suhadipc/public_html/web_sipemo2/
    - /usr/bin/rsync -av --delete --exclude='.env' --exclude='vendor/' --exclude='storage/' --exclude='.git/' ./ $DEPLOYPATH
```
